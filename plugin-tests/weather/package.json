{
  "name": "weather",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "cp ./plugin.yaml ./dist/plugin.yaml && tsup src/index.ts --format cjs --dts",
    "dev": "cp ./plugin.yaml ./dist/plugin.yaml && tsup src/index.ts --format cjs --dts --watch"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
